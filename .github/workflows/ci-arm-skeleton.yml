name: ARM Smoke (skeleton)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  smoke-arm-skeleton:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Note
        run: echo "This is a skeleton ARM/QEMU job. See .github/workflows/ci.yml for full job."
